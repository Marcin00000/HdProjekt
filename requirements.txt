# Faza 0 — fundament
python-dotenv>=1.0.0
pandas>=2.0.0
pyarrow>=14.0.0
fsspec>=2024.2.0
adlfs>=2024.4.0
sqlalchemy>=2.0.0
pyodbc>=5.0.0
dvc[azure]>=3.0.0

# Faza 1 — EDA
matplotlib>=3.8.0
pyyaml>=6.0.0

# Fazy 1–5 (EDA, ML, orkiestracja) — instalowane od razu dla spójnego venv
scikit-learn>=1.4.0
xgboost>=2.0.0
mlflow>=2.10.0
prefect>=2.14.0

# Faza 6–7 — API + portal web
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
jinja2>=3.1.0
httpx>=0.27.0
pytest>=8.0.0

# Faza 8 — monitoring driftu
evidently>=0.4.0
